# Optional — overrides API base (must match how the browser reaches FastAPI).
# VITE_API_BASE_URL=http://127.0.0.1:8000
#
# In local dev, if unset, the app uses the Vite proxy: /api -> http://127.0.0.1:8000
# (run uvicorn on 8000 and npm run dev on 5173).
